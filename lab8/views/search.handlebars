<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/public/site.css">
    <title>Shows Found</title>
    <h1> Shows Found</h1>
    <h2 class = "centered">Search term: {{searchTerm}} </h2>
  </head>
  <body class= "centered">
      {{#if shows.length}}
        <ul>
            {{#each shows}}
            <li >
                 <a href = "/shows/{{show.id}}"> {{show.name}} </a>
            </li>
            {{/each}}
        </ul>
      {{else}}
        <p class="not-found">We're sorry, but no results were found for {{searchTerm}}.</p>
      {{/if}}

  </body>
  <footer class="centered">
      <a href="http://localhost:3000/" > Make another Search</a>
  </footer>
</html>